(in-package #:noloop.cacau)

(defmacro ui-cl ()
  (defun suite (name &key timeout parent)
    )

  (defun test (name test-form &key timeout)
    )

  (defun run ()
    ))

(let ((suites '()))
  (ui-cl))
